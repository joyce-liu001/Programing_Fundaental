// z5271698

#include <stdio.h>

#define LARGEST_INTEGER 99

int main(void) {
    char array[MAX];
    fgets(array, MAX, stdin); 
    
    
    int i = 0
    while (i < 26) {
    printf("%c %d %d\n", leteer[i], p, times);
    i++;
    }
}
